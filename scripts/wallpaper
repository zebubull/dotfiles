#!/usr/bin/bash

set -e

name=${1%.*}

if [[ -f "$name.json" ]]; then
    wal --cols16 darken -f "$name.json" -a 85
else
    wal --cols16 darken -i "$1" -a 85
fi
makoctl reload
eww reload
eww close bar
eww open bar
swww img -t wipe $1
